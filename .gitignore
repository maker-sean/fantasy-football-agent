node_modules/

# Secrets — never commit
.env
.env.*
!.env.example

# Runtime state and captured message content.
# Created at runtime by observer.js and poller.js, so nothing to keep here.
logs/

.DS_Store
*.log
